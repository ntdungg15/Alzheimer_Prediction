# Build tools - install first
setuptools>=68.0.0
wheel>=0.40.0

# Web framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# Image processing
pillow>=10.0.0

# Core ML libraries
numpy>=1.24.0,<2.0.0

# Choose ONE of these ML frameworks:
# Option 1: PyTorch (uncomment if using PyTorch)
torch>=2.1.0
torchvision>=0.16.0

# Option 2: TensorFlow (for Inception V3 model từ Kaggle)
tensorflow>=2.13.0 
